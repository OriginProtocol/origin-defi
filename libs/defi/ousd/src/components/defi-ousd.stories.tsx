import type { Meta } from '@storybook/react';
import { DefiOusd } from './defi-ousd';

const Story: Meta<typeof DefiOusd> = {
  component: DefiOusd,
  title: 'DefiOusd',
};
export default Story;

export const Primary = {
  args: {},
};
