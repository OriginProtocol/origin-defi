query WithdrawalRequests($address: String!) {
  oethWithdrawalRequests(where: { withdrawer_eq: $address }) {
    id
    requestId
    timestamp
    amount
    queued
    claimed
    blockNumber
    requestId
    txHash
  }
}
