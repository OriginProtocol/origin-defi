import type { Meta } from '@storybook/react';
import { DefiOeth } from './defi-oeth';

const Story: Meta<typeof DefiOeth> = {
  component: DefiOeth,
  title: 'DefiOeth',
};
export default Story;

export const Primary = {
  args: {},
};
