query Proposals {
  ogvProposals(orderBy: timestamp_DESC, limit: 1000) {
    id    
    description
    timestamp
    startBlock
    endBlock
    lastUpdated
    status    
  }
}

query HoldersCount {
  ogvAddressesConnection(orderBy: id_ASC) {
    totalCount
  }
}
